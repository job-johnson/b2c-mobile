<?php
/**
 * Helcim Gateway install script
 */

$installer = $this;

// Add gateway order id to store the order id returned by the gateway for cross-reference
//$installer->addAttribute('quote_payment', 'gateway_order_id', array());
//$installer->addAttribute('order_payment', 'gateway_order_id', array());
//$installer->addAttribute('invoice', 'gateway_order_id', array());